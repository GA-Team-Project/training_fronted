{{#each books as |book|}}
  {{log book}}
<!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <h1 data-id="{{book.id}}" class="my-book"> <small>{{book.title}}</small></h1>
  <h4 data-id="{{book.id}}">
    {{#if book.author}}
      {{book.author}}
    {{else}}
      No author provided.
    {{/if}}
  </h4>
  <p><button type="submit">Remove</button></p>


{{/each}}
